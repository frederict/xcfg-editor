/**
 * `sharingDialog.ts`, `warnings.ts` — donner son fichier à quelqu'un, et ce qu'il faut en
 * savoir avant.
 *
 * **Encore vide** : ce domaine attend son lot d'extraction. Voir `widgets.ts` pour
 * pourquoi le fichier existe quand même.
 */
const sharing = {} as const

export default sharing

export type FrenchSharing = typeof sharing
