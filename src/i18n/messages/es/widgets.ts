import type { DomainCatalog } from '../../domains'

/** `properties.ts`, `widgetPalette.ts`, `widgetList.ts` — encore vide, voir `fr/widgets.ts`. */
const widgets: DomainCatalog<'widgets'> = {}

export default widgets
